export default function ComingSoon() {
  return (
    <div className="flex items-center justify-center h-full min-h-[80vh]">
      <h1
        className="font-bold tracking-tight select-none"
        style={{ fontSize: "clamp(4rem, 12vw, 9rem)", color: "#ececec" }}
      >
        Prochainement
      </h1>
    </div>
  );
}
